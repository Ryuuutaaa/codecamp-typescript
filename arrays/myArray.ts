const superHero: string[] = [];

const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUser: User[] = [];

const MLModels: number[][] = [[255, 255, 255], []];

superHero.push("Rafi akmal");
heroPower.push(10);

allUser.push({ name: "", isActive: true });
